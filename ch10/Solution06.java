package chapter10;

public class Solution06 {
	//Since the file is 20GB, it cannot be sorted in memory.
	//Therefore, the file can be divided into many chunks with size x, which can fit into the memory.
	//After every chunk is sorted, the sorted chunks are merged together to generate a sorted file.
}
